%p
	You have requested a password reset. To reset your password please follow this link:
	
	%a{:href => "#{@app_url}/#passwords/new?secret=#{@reset.secret}"}
		= "#{@app_url}/#passwords/new?secret=#{@reset.secret}"
	
	This link will expire one hour from time the password reset was initiated.